{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build DevContainer",
      "type": "shell",
      "command": "docker buildx build --pull --rm --output type=docker -f \"images/bun/Dockerfile\" -t workspace:latest \"images/bun\"",
      "problemMatcher": []
    }
  ]
}
