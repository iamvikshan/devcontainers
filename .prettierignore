node_modules/
*ignore*
*.nix
*lock*

# Exclude setup.zsh files with complex shell arithmetic that prettier-plugin-sh can't parse
images/**/setup.zsh

# Exclude Dockerfiles under images due to known parser/plugin limitation
images/**/Dockerfile
