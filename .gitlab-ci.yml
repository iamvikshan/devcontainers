# .gitlab-ci.yml
# Sync is now handled by GitHub Actions using advanced-git-sync
# This file is kept for future GitLab CI/CD needs

stages:
  - placeholder

placeholder-job:
  stage: placeholder
  script:
    - echo "GitLab CI/CD placeholder - sync handled by GitHub Actions"
  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[run-gitlab-ci\]/
