name: Release DevContainers

on:
  workflow_dispatch:
  schedule:
    # Run every Sunday at 2 AM UTC
    - cron: "0 2 * * 0"
  push:
    branches: [main]

env:
  GITHUB_REGISTRY: ghcr.io
  GITLAB_REGISTRY: registry.gitlab.com
  GL_USERNAME: vikshan

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN }}

      - name: Setup Bun and install dependencies
        uses: iamvikshan/.github/.github/actions/setup-bun@main

      - name: Pre-release check
        id: pre_release_check
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "🔍 Checking if release is needed..."

          # Run pre-release check in workflow mode (outputs only JSON)
          RESULT=$(bun scripts/pre-release-check.ts --workflow)
          echo "📋 Pre-release check result: $RESULT"

          # Parse the JSON result
          SHOULD_RELEASE=$(echo "$RESULT" | jq -r '.shouldRelease')
          RELEASE_TYPE=$(echo "$RESULT" | jq -r '.releaseType')
          NEXT_VERSION=$(echo "$RESULT" | jq -r '.nextVersion // empty')

          if [ "$SHOULD_RELEASE" = "true" ]; then
            echo "release_needed=true" >> $GITHUB_OUTPUT
            echo "release_type=$RELEASE_TYPE" >> $GITHUB_OUTPUT

            if [ "$RELEASE_TYPE" = "forced" ]; then
              echo "🚀 Base image updates detected - will force a release"
            else
              echo "✅ Semantic release will create a new version"
            fi

            # Get predicted version for container building if not already available
            if [ -n "$NEXT_VERSION" ] && [ "$NEXT_VERSION" != "null" ]; then
              echo "predicted_version=$NEXT_VERSION" >> $GITHUB_OUTPUT
              echo "📋 Predicted version: $NEXT_VERSION"
            else
              PREDICTED_VERSION=$(bun run predict-version)
              if [ "$PREDICTED_VERSION" != "no-version" ]; then
                echo "predicted_version=$PREDICTED_VERSION" >> $GITHUB_OUTPUT
                echo "📋 Predicted version: $PREDICTED_VERSION"
              fi
            fi
          else
            echo "release_needed=false" >> $GITHUB_OUTPUT
            echo "ℹ️  No release needed"
          fi

      - name: Create force release commit
        if: steps.pre_release_check.outputs.release_needed == 'true' && steps.pre_release_check.outputs.release_type == 'forced'
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "📝 Creating force release commit for base image updates..."

          # Configure git
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # Create the force release commit
          bun run pre-release-check --create-commit

          echo "✅ Force release commit created"

      - name: Configure registries
        if: steps.pre_release_check.outputs.release_needed == 'true'
        uses: ./.github/actions/configure-registries
        with:
          github-pat: ${{ secrets.GH_TOKEN }}
          gitlab-token: ${{ secrets.GITLAB_TOKEN }}
          github-registry: ${{ env.GITHUB_REGISTRY }}
          gitlab-registry: ${{ env.GITLAB_REGISTRY }}
          github-username: ${{ github.actor }}
          dockerhub-username: ${{ env.GL_USERNAME }}
          dockerhub-token: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build containers with predicted version
        if: steps.pre_release_check.outputs.release_needed == 'true' && steps.pre_release_check.outputs.predicted_version != ''
        uses: ./.github/actions/build-containers
        with:
          version: ${{ steps.pre_release_check.outputs.predicted_version }}
          github-registry: ${{ env.GITHUB_REGISTRY }}
          gitlab-registry: ${{ env.GITLAB_REGISTRY }}
          gitlab-project-path: ${{ env.GL_USERNAME }}/${{ github.event.repository.name }}
          dockerhub-username: ${{ env.GL_USERNAME }}

      - name: Update VERSIONS.md with real-time data
        if: steps.pre_release_check.outputs.release_needed == 'true' && steps.pre_release_check.outputs.predicted_version != ''
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "⏳ Waiting for images to be available across all registries..."
          sleep 30

          echo "📝 Updating VERSIONS.md with real-time data..."
          bun run update-versions --version=${{ steps.pre_release_check.outputs.predicted_version }} --notes="Automated release with latest base images,Security patches and updates,Documentation improvements"

          echo "📋 Staging VERSIONS.md and README files for semantic release..."
          git add VERSIONS.md README.md base/*/README.md

      - name: Semantic Release
        if: steps.pre_release_check.outputs.release_needed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          GIT_AUTHOR_NAME: ${{ github.actor }}
          GIT_AUTHOR_EMAIL: ${{ github.actor }}@users.noreply.github.com
          GIT_COMMITTER_NAME: ${{ github.actor }}
          GIT_COMMITTER_EMAIL: ${{ github.actor }}@users.noreply.github.com
        run: |
          echo "🚀 Running semantic release..."
          bunx semantic-release

      - name: No release needed
        if: steps.pre_release_check.outputs.release_needed != 'true'
        run: |
          echo "ℹ️  No release needed - all images are up to date and no code changes require a release"
          echo "✅ Workflow completed successfully"
