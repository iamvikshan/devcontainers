name: Release DevContainers

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "base/**"

env:
  GITHUB_REGISTRY: ghcr.io
  GITLAB_REGISTRY: registry.gitlab.com
  GITHUB_IMAGE_NAME: ${{ github.repository }}
  GITLAB_PROJECT_PATH: vikshan/devcontainers
  GITLAB_PROJECT_ID: 64626588

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Log in to GitHub Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GITHUB_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GH_PAT }}

      - name: Log in to GitLab Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GITLAB_REGISTRY }}
          username: oauth2
          password: ${{ secrets.GITLAB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Semantic Release
        id: semantic_release
        run: npx semantic-release
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          GIT_AUTHOR_NAME: vixshan
          GIT_AUTHOR_EMAIL: vixshan@gmail.com
          GIT_COMMITTER_NAME: vixshan
          GIT_COMMITTER_EMAIL: vixshan@gmail.com

      - name: Get latest release tag
        id: get_release_tag
        run: |
          VERSION=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/latest" | \
            jq -r '.tag_name' | sed 's/v//')
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Build and push base containers
        run: |
          for dir in base/*/; do
            devcontainer_name=$(basename "$dir")
            if [ -f "$dir/.devcontainer/Dockerfile" ]; then
              # Build and push to GitHub
              docker buildx build \
                --file "$dir/.devcontainer/Dockerfile" \
                --push \
                --tag "${GITHUB_REGISTRY}/${GITHUB_IMAGE_NAME}/${devcontainer_name}:v${VERSION}" \
                --tag "${GITHUB_REGISTRY}/${GITHUB_IMAGE_NAME}/${devcontainer_name}:latest" \
                "$dir/.devcontainer"

              # Build and push to GitLab
              docker buildx build \
                --file "$dir/.devcontainer/Dockerfile" \
                --push \
                --tag "${GITLAB_REGISTRY}/${GITLAB_PROJECT_PATH}/${devcontainer_name}:v${VERSION}" \
                --tag "${GITLAB_REGISTRY}/${GITLAB_PROJECT_PATH}/${devcontainer_name}:latest" \
                "$dir/.devcontainer"
            fi
          done

      - name: Build and push Ubuntu containers
        run: |
          for dir in base/ubuntu/*/; do
            devcontainer_name=$(basename "$dir")
            if [ -f "$dir/Dockerfile" ]; then
              # Build and push to GitHub
              docker buildx build \
                --file "$dir/Dockerfile" \
                --push \
                --tag "${GITHUB_REGISTRY}/${GITHUB_IMAGE_NAME}/ubuntu-${devcontainer_name}:v${VERSION}" \
                --tag "${GITHUB_REGISTRY}/${GITHUB_IMAGE_NAME}/ubuntu-${devcontainer_name}:latest" \
                "$dir"

              # Build and push to GitLab
              docker buildx build \
                --file "$dir/Dockerfile" \
                --push \
                --tag "${GITLAB_REGISTRY}/${GITLAB_PROJECT_PATH}/ubuntu-${devcontainer_name}:v${VERSION}" \
                --tag "${GITLAB_REGISTRY}/${GITLAB_PROJECT_PATH}/ubuntu-${devcontainer_name}:latest" \
                "$dir"
            fi
          done

      - name: Create GitLab Release and Tag
        run: |
          # Create tag first
          git tag "v${VERSION}" || true
          git push https://oauth2:${{ secrets.GITLAB_TOKEN }}@gitlab.com/${GITLAB_PROJECT_PATH}.git "v${VERSION}" || true
          
          # Get GitHub release details
          GITHUB_RELEASE=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/latest")
          
          RELEASE_NOTES=$(echo $GITHUB_RELEASE | jq -r '.body')
          if [ "$RELEASE_NOTES" = "null" ]; then
            RELEASE_NOTES="Release v${VERSION}"
          fi
          
          # Create GitLab release
          RESPONSE=$(curl --request POST \
            --header "PRIVATE-TOKEN: ${{ secrets.GITLAB_TOKEN }}" \
            --header "Content-Type: application/json" \
            --data "{
              \"name\": \"v${VERSION}\",
              \"tag_name\": \"v${VERSION}\",
              \"description\": ${RELEASE_NOTES@J}
            }" \
            "https://gitlab.com/api/v4/projects/${GITLAB_PROJECT_ID}/releases")

          # Verify response
          if ! echo "$RESPONSE" | jq -e '.tag_name' > /dev/null; then
            echo "Failed to create GitLab release:"
            echo "$RESPONSE"
            exit 1
          fi

      - name: Update image sizes
        run: |
          echo "GITLAB_TOKEN=${{ secrets.GITLAB_TOKEN }}" > .env
          npm run s && npm run f

      - name: Commit README updates
        run: |
          git config --global user.email "vixshan@gmail.com"
          git config --global user.name "vixshan"
          git add README.md base/*/README.md
          git commit --amend --no-edit
          git push -f origin HEAD:${{ github.ref_name }}

          # Push to GitLab
          git remote add gitlab https://oauth2:${{ secrets.GITLAB_TOKEN }}@gitlab.com/${GITLAB_PROJECT_PATH}.git
          git push -f gitlab HEAD:${{ github.ref_name }}
